- @result.each do |recipe|
    li 
      a href="/recipes_browse/recipe/#{recipe['id']}" #{recipe["name"]}
      form action='/user/saved_recipes/#{recipe['id']}/remove' method='post'
            input type='submit' value='Remove'